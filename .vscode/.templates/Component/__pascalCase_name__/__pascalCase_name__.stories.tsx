import React from 'react';
import { {{ pascalCase name }} } from "./{{ pascalCase name }}";
import { storiesOf } from '@storybook/react';

const story = storiesOf('Components/{{ pascalCase name }}', module);

story.add(
    '{{ pascalCase name }} Component',
    () => <{{ pascalCase name }} />
);
